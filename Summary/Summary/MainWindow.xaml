<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Summary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Summary"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="RootTitleBar"
                  Title="{StaticResource ApplicationTitle}"
                  Grid.Row="0"
                  IsPaneToggleButtonVisible="True"
                  PaneToggleRequested="RootTitleBar_PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/StoreLogo.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView x:Name="RootNavigationView"
                        IsBackButtonVisible="Collapsed"
                        IsPaneToggleButtonVisible="False"
                        SelectionChanged="RootNavigationView_SelectionChanged"
                        Grid.Row="1">
            <Frame x:Name="RootFrame" />
        </NavigationView>
    </Grid>
</Window>